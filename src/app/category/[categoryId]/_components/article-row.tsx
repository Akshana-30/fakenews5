import NewsCard from "@/components/news-card";
import { Article } from "@/lib/types";

export default function ArticleRow({ articles }: { articles: Article[] }) {
    return (
        <div>
            {articles.map((a, i) => {
                
        </div>
    )
}